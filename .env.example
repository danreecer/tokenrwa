# AI provider — required for live analysis. Without it, the app runs in demo mode
# (curated demo Passports for OUSG, BUIDL and USDY still work).
GEMINI_API_KEY=

# Optional: override the Gemini model (default: gemini-flash-latest)
# GEMINI_MODEL=gemini-flash-latest
